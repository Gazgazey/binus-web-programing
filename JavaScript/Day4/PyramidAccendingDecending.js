for (let a = 7; a > 0; a--) {
    let right = "";
    for (let b = 0; b < a; b++) {
        right += a;
    }
    console.log(right);
}

for (let a = 2; a < 8; a++) {
    let right = "";
    for (let b = 0; b < a; b++) {
        right += a;
    }
    console.log(right);
}
